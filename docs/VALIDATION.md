# 当前验证状态与验收清单

[文档导航](README.md) · [构建指南](BUILD.md) · [历史记录](history/VALIDATION-2026-09.md)

本文按已取得的证据整理当前状态。**支持构建选项、离线测试通过、SDK 编译通过和设备认证通过是不同阶段。** 未提供新的运行结果时，不将待验证项自动升级为通过。

## 当前证据

当前功能基线为 `3231a93`（21.02.7 核心表达式版本解析修复）。本次目录与文档整理不修改运行代码。

| 范围 | 已有证据 | 尚缺的验证 |
| --- | --- | --- |
| 构建辅助工具 | 14 项 Python 测试通过；覆盖 25 个组合、50 个模拟产物、版本/依赖拒绝、ELF 架构、源码比对和 Makefile 解析 | 真实 SDK 工具链及产物验证 |
| 设置页兼容入口 | Lua 5.1 模拟新版、旧版和接口缺失三种情况通过 | 真实 LuCI 中显示、保存和应用 |
| 静态检查 | Lua/Bash 语法、Actionlint、ShellCheck 和 Git 差异格式检查通过 | 不代替运行测试 |
| ImmortalWrt 21.02.7 / aarch64_cortex-a53 | 用户日志确认 LuCI IPK 已生成，随后旧版本校验器报 Missing PKG_VERSION；该解析问题已修复并通过离线测试 | 修复后重新运行并通过完整产物校验 |
| ImmortalWrt 21.02.7 其余四架构 | 官方 SDK 文件名、SHA256、软件包架构已核对 | 最新代码的完整 SDK 构建 |
| 现有 ImmortalWrt / OpenWrt 24.10、25.12 组合 | 离线回归覆盖所有配置；早期构建反馈不代表当前提交已验收 | 兼容入口和校验变更后的真实构建回归 |
| 路由器功能 | 原排错会话有菜单、安装检测和日志恢复反馈 | 本仓库最终源码的完整设备验收及校园网认证 |

Python 测试中的 IPK/ELF 为合成数据，APK 解码使用模拟；Lua 测试也使用模拟 dispatcher。历次测试数量、初次构建失败过程和早期设备反馈保留在 [历史记录](history/VALIDATION-2026-09.md)，不作为当前全面通过的证明。

## 构建验收

- [ ] 从包含修复的最新提交新建 `immortalwrt-21.02.7 / all`，验收五种架构；也可先用 x86_64 做单架构检查。
- [ ] 对其余四个系统选项各构建一个代表架构，确认公共逻辑无回归；需声明全架构支持已验证时，再覆盖各自 all。
- [ ] 每个成功任务只有核心和本仓库界面两个附件，包名、版本、依赖、架构及文件内容检查均通过。
- [ ] 保存运行链接、提交号、系统选项、架构、实际包版本及结果；重跑旧提交不能证明新代码正确。

## 设备验收

对每个实际测试的系统记录 `DISTRIB_ID`、版本、软件包架构、包版本及对应构建提交；不要记录真实密码。

- [ ] 安装匹配的 APK/IPK、依赖解析正常，重新登录后四页均可打开。
- [ ] 安装版本显示正确，无 WAN 或无 IP 时状态页正常。
- [ ] 设置可保存和应用，服务读取到预期配置。
- [ ] 下线、重拨生效，页面与接口无旧路径 404。
- [ ] 日志初次加载、定时刷新、失败提示和中文编码正常。
- [ ] 网络探测在无网络、未登录、联网三种状态下结果合理。
- [ ] 调试包可下载并解包，分享前检查和脱敏。
- [ ] 使用真实校园网完成认证；页面正常不等于认证成功。

## 已知运行限制

这些是已有代码行为，本次文档整理没有修复：

- 状态页明文显示设备中保存的账号、密码，截图可能泄露凭据。
- 调试包收集认证、无线、网络等完整配置，可能包含校园网和 Wi-Fi 密码。
- 下线和重拨使用 GET；网络探测依赖外部 HTTP 服务和旧 XHR。
- 日志接口声明 GBK，而 fetch.text 使用 UTF-8，中文显示仍需实机确认。

源码与 Git 历史的敏感信息扫描未检出密钥，不代表这些运行时数据可以公开。未来修改相关行为时，应同时补充功能与权限测试。

## 如何更新记录

在上方表格更新结果时附上证据，不覆盖或推断历史事实。较长日志与阶段记录放入 `history/`，注明日期、提交、环境和验证边界；不要把真实配置或原始调试包作为仓库附件。
